# U1000 Top1000 training authorization decision

## GO FOR SPLIT CREATION

Classification is **C: serialized-record provenance defect that does not alter tensors**.

The production dataset remains immutable at SHA-256 `6a794c38216175212ec969283fdbd214bcdc64caecde78d24b81545e681a332a`. Its stale preprocessing label appears in the sidecar and all serialized universe/snapshot records, but the full dataset scan and validator establish the intended U1000, five-snapshot, `[1000,7]`, normalization-none, Top1000, sparse-edge-index, periodic kNN k=8, box-25 protocol. Targets and source files match their full-SHA256 identities.

Neither trainer consumes or parses `preprocessing_version`. Graph dispatch comes from sparse edge keys/storage; shapes and features come from tensors; split identity comes from immutable checksums and ordered universe IDs. The label therefore cannot change model behavior or scientific tensors.

No artifact correction was performed. Dataset, sidecar, and completion-marker checksums are unchanged; the dedicated validator returned `PASS`; the focused regression suite passed 14/14 tests. Future temporal builds now generate labels from effective configuration and assert sidecar/universe/snapshot consistency.

Authorization is limited to the next gate: create reviewed split manifests bound to the dataset checksum above and the recorded source/target identities. This audit does **not** authorize training, rebuilding, kNN execution, predictions, checkpoints, or an artifact migration.

Exact next action: create the approved seed/level split manifests using the immutable dataset checksum and ordered IDs `LH_0..LH_999`, then separately review them before any training authorization.
